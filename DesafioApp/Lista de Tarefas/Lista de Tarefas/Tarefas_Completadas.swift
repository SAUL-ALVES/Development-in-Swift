import SwiftUI

// Tela de Tarefas Completadas
struct CompletedTasksView: View {
    var body: some View {
        Text("Aqui vão as tarefas completadas")
            .font(.largeTitle)
            .padding()
    }
}

